#include <new>
#include <stdlib.h>
#include <iostream>

int main() {
    char chars[2];
    std::cout << chars;
    // set_terminate
    // std::set_new_handler()
    // std::exit(0);
    // std::abort();
    return EXIT_SUCCESS;
}