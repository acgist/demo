#include <iostream>

#include "torch/torch.h"
#include "torch/script.h"

int main() {
    // long long ai[] { 1, 2, 3 };
    // auto a = torch::from_blob(ai, {3, 1}, torch::kLong);
    // std::cout << a << "\n";
    // long long bi[] { 1, 2, 3 };
    // auto b = torch::tensor(bi, torch::kLong);
    // std::cout << b << "\n";
    // auto c = torch::tensor({ 1, 2, 3 }, torch::kLong);
    // std::cout << c << "\n";
    // auto a = torch::ones({1, 2});
    // auto b = torch::ones({2, 3});
    // std::cout << (a * a).sizes() << "\n";
    // std::cout << (a.mul(a)).sizes() << "\n";
    // // std::cout << (a.dot(b)).sizes() << "\n";
    // std::cout << (torch::mm(a, b)).sizes() << "\n";
    // std::cout << (torch::mm(a, b)) << "\n";
    return 0;
}
